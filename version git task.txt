Git is a version control system that tracks changes in files and coordinates work on those files among multiple people.

False 

git --version

--global

git config user.email "medogadalrb@gmail.com"

git add --all

git status

git init

False

git commit -m "message"

git commit -m "New email"

git log

A separate version of the main repository

git branch new-email

git checkout new-email

git merge new-email